import Link from "next/link";
import { CheckCircle2, ExternalLink, ShieldCheck } from "lucide-react";
import { Brand } from "@/components/brand";

export default async function ProofPage() {
  const signature = process.env.NEXT_PUBLIC_SOLANA_PROOF_SIGNATURE;
  return <main className="proof-page"><nav className="nav-wrap"><Brand /><Link className="button dark" href="/runs/demo">Open product</Link></nav><section><span className="kicker"><i /> Verifiable operation</span><h1>Proof, not a promise.</h1><p>Each item below names what is genuinely active in the public deployment and where its evidence comes from.</p><div className="proof-grid"><article><CheckCircle2 /><span><b>Live model decisions</b><small>Groq primary and quota-aware fallback, server-side only</small></span></article><article><CheckCircle2 /><span><b>Real Chromium execution</b><small>Playwright observes, acts, and captures a JPEG on Vercel</small></span></article><article><ShieldCheck /><span><b>Deterministic action safety</b><small>Payment completion and sensitive data are checked before execution</small></span></article><article><CheckCircle2 /><span><b>Solana ownership verification</b><small>Expiring message signatures verified against the claimed public key</small></span></article></div><div className="proof-links"><a href="/api/health">Open live health JSON <ExternalLink size={14} /></a>{signature && <a href={`https://explorer.solana.com/tx/${signature}?cluster=devnet`} target="_blank" rel="noreferrer">Open devnet launch receipt <ExternalLink size={14} /></a>}<a href="https://github.com/bryankwandou/mirrorqa-ai/actions" target="_blank" rel="noreferrer">Open CI history <ExternalLink size={14} /></a></div><aside><b>Deliberately not claimed</b><p>The public MVP does not claim persistent multi-tenant storage, Clerk organization auth, deploy-trigger scheduling, or arbitrary-domain durable workers. Those need their production services and are not replaced with fabricated rows or controls.</p></aside></section></main>;
}
