import Link from "next/link";
import { Brand } from "@/components/brand";

export default function DocsPage() { return <main className="docs-page"><aside><Brand /><nav><a href="#start">Start here</a><a href="#agent">Agent loop</a><a href="#safety">Safety architecture</a><a href="#solana">Solana proof</a><a href="#limits">Deployment limits</a></nav><Link href="/dashboard">Open interactive demo →</Link></aside><article><section id="start"><span className="eyebrow">MirrorQA documentation</span><h1>Test customer understanding, not just functional correctness.</h1><p>Register one public web journey, state a concrete outcome, and select the customer profiles that matter. Each run stores an ordered trace so every finding remains connected to what happened.</p></section><section id="agent"><h2>Agent loop</h2><p>The agent receives a structured observation of visible and accessible controls, the customer goal, its fixed persona policy, and prior actions. It proposes exactly one next action. The browser layer resolves that description against accessible names and executes only after validation.</p><pre><code>observe → decide → validate → execute → capture → repeat</code></pre></section><section id="safety"><h2>Safety architecture</h2><p>Prompt instructions are not enforcement. MirrorQA checks every proposed action using deterministic rules immediately before execution. Purchase completion and sensitive personal-data inputs are rejected and added to the immutable trace.</p><ul><li>Payment labels are normalized and matched against completion intent.</li><li>Sensitive fields are detected from labels, names, placeholders, roles, and input types.</li><li>Clearly marked sandbox values are permitted; real-looking values are blocked.</li><li>An unrelated confirmation control remains allowed to avoid a blanket false positive.</li></ul></section><section id="solana"><h2>Solana devnet proof</h2><p>The run screen can connect Phantom or Solflare, verify wallet ownership with a signed expiring message, and write a Memo transaction from the user’s wallet. The receipt endpoint fetches the transaction from devnet and verifies the MirrorQA payload independently.</p></section><section id="limits"><h2>Honest deployment limits</h2><p>Vercel hosts the product and short server functions. A genuine multi-minute Playwright session should run in an isolated container worker connected through durable orchestration. The current public MVP exposes a real Groq decision endpoint and real safety enforcement, but does not label static fixture screenshots as live browser captures.</p></section></article></main>; }
