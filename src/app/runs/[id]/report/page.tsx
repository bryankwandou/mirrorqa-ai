import Link from "next/link";
import { ArrowLeft, ArrowUpRight, Download, Link2 } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { findings } from "@/lib/data";

export default function ReportPage() {
  return <AppShell active="runs"><div className="page-content report-page"><Link className="back-link" href="/runs/demo"><ArrowLeft size={15} /> Back to run trace</Link><div className="page-title"><div><span className="eyebrow">Friction report / MQ-028</span><h1>Price is visible. Commitment is not.</h1><p>The journey creates uncertainty around total cost and changes the language of commitment at the payment boundary.</p></div><div className="report-actions"><button className="button ghost"><Link2 size={15} /> Copy link</button><button className="button dark"><Download size={15} /> Export</button></div></div><div className="report-summary"><article><span>Experience score</span><strong>71<small>/100</small></strong></article><article><span>Outcome</span><strong className="medium-text">Stopped safely</strong><small>At payment completion</small></article><article><span>Findings</span><strong>3</strong><small>1 high, 1 medium, 1 low</small></article><article><span>Persona</span><strong className="medium-text">Price-sensitive</strong><small>Profile 02</small></article></div><div className="report-layout"><section><div className="report-section-head"><span>Evidence-backed findings</span><small>Ordered by severity</small></div>{findings.map((finding, index) => <article className="finding-card" key={finding.title}><div className="finding-top"><span className={`severity ${finding.severity}`}>{finding.severity}</span><span>Finding {String(index + 1).padStart(2,"0")}</span></div><h2>{finding.title}</h2><p>{finding.detail}</p><div className="recommendation"><span>Recommended change</span><p>{index === 0 ? "Place overage rules beside the monthly price and state the maximum possible charge before account creation." : index === 1 ? "Keep the trial language consistent through the final step and explain why payment details are requested before showing a subscription confirmation control." : "Use one unit across plan limits and expose differences in a fixed comparison row."}</p></div><Link href={`/runs/demo#step-${finding.step}`}>View cited step {String(finding.step).padStart(2,"0")} <ArrowUpRight size={14} /></Link></article>)}</section><aside><div className="panel report-context"><span className="eyebrow">Run context</span><dl><div><dt>Flow</dt><dd>Trial to checkout</dd></div><div><dt>Started</dt><dd>22 Jul, 09:42 UTC</dd></div><div><dt>Duration</dt><dd>21 seconds</dd></div><div><dt>Page</dt><dd>northstar.app/pricing</dd></div><div><dt>Guardrail</dt><dd className="positive">Enforced</dd></div></dl></div><div className="panel traceability"><span className="eyebrow">Traceability</span><h3>No unsupported advice.</h3><p>Every statement in this report links to a specific observed step. Generic best-practice filler is excluded.</p></div></aside></div></div></AppShell>;
}
