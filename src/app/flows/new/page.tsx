import { ArrowLeft, Check, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { personas } from "@/lib/data";

export default function NewFlowPage() {
  return <AppShell active="flows"><div className="form-page"><Link className="back-link" href="/flows"><ArrowLeft size={15} /> Back to flows</Link><div className="page-title"><div><span className="eyebrow">New customer journey</span><h1>What should customers accomplish?</h1><p>Define one concrete outcome. MirrorQA will attempt it from each selected perspective.</p></div></div><form className="flow-form"><section className="form-section"><span className="form-index">01</span><div><h2>Journey details</h2><p>Use a publicly reachable staging or production URL.</p></div><div className="field-stack"><label>Starting URL<input type="url" placeholder="https://yourproduct.com/pricing" /></label><label>Customer goal<textarea placeholder="Start a free trial and reach the workspace dashboard" rows={3} /></label><label>Maximum steps<input type="number" defaultValue={15} min={3} max={30} /></label></div></section><section className="form-section"><span className="form-index">02</span><div><h2>Choose perspectives</h2><p>Each profile follows a separate decision policy.</p></div><div className="persona-select">{personas.map((persona, index) => <label key={persona.key}><input type="checkbox" defaultChecked={index < 3} /><span className={`persona-number ${persona.color}`}>{persona.mark}</span><span><b>{persona.name}</b><small>{persona.note}</small></span><Check size={15} /></label>)}</div></section><section className="attestation"><ShieldCheck size={21} /><label><input type="checkbox" /><span><b>I am authorized to test this website.</b><small>I understand that MirrorQA drives a real browser while blocking payment completion and sensitive personal-data entry.</small></span></label></section><div className="form-actions"><Link className="button ghost" href="/flows">Cancel</Link><Link className="button primary" href="/runs/demo">Register and start run</Link></div></form></div></AppShell>;
}
